# Keyless real-Loader composition for first-match instruction selection. The
# seeded session resumes under `first-existing` selection: `CLAUDE.md` wins any
# directory where both siblings exist, and a selection-mode change supersedes a
# visible baseline seeded under the default `all-existing` semantics.

- id: persistence
  name: '@deepseek-ai/dsh-session-persistence-jsonl'
  config:
    root: './.sessions'
    compression: none

- id: replay
  name: '@deepseek-ai/dsh-llm-replay'
  config:
    file: !!js process.env.DSH_SNAPSHOT_FILE
    overrideFile: !!js process.env.DSH_SNAPSHOT_OVERRIDE

- id: fs-local
  name: '@deepseek-ai/dsh-fs-local'
  config:
    cwd: !!js process.cwd()

- id: agent
  name: '@deepseek-ai/dsh-agent-spine-demo'
  config:
    agents: []
    workspaceContext:
      maxBytes: 65536
      dshHome: !!js process.cwd() + '/.dsh'
      instructionFileCandidates: ['CLAUDE.md', 'AGENTS.md']
      candidateSelection: first-existing
    skills:
      enabled: false
    toolBash: false
    toolJobs: false
    goals: false

# Await the persisted resume before the headless driver inspects root agents.
- id: resumed-agent
  name: './tests/fixtures/workspace-context-resume-agent.ts'
